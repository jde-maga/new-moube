import { css } from "emotion";

const layoutContainer = css(`
  height: 100vh;
`);

const contentContainer = css(`
  height: 100%;
`);

const dataContainer = css(`
  padding: 2rem;
`);

export default {
  layoutContainer,
  contentContainer,
  dataContainer
}